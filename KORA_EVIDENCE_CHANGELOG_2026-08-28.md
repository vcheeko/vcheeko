# KORA Evidence Changelog — 2026-08-28

## Added evidence level

The sanitized public evidence surface now records that private CI has exercised the bounded Golden persistence path through:

- a real ephemeral PostgreSQL engine;
- a least-privilege database boundary that denies direct persistence-table authority;
- a real Node-to-PostgreSQL protocol-driver path through that restricted interface;
- exact replay without duplicate worker execution;
- application-side SQL allowlisting;
- full repository validation including tests, typecheck and production build.

## Claim boundary

This update does **not** claim production deployment, public reproduction, independent acceptance, production credentials, arbitrary executor authority or pilot reliability.

The public Golden Demo status remains **prototype / evidence-building — not verified for release**.
